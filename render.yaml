services:
  - type: web
    name: mtr01-estacion
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python codigo_python_viento.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11